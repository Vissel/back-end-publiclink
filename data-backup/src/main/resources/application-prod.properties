server.address=0.0.0.0
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/backup_meta?useSSL=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
backup.source.publiclink.url=jdbc:mysql://${PUBLICLINK_DB_HOST}:${PUBLICLINK_DB_PORT:3306}/publiclink-db?useSSL=true&serverTimezone=UTC&readOnly=true
backup.source.publiclink.username=${PUBLICLINK_DB_USERNAME}
backup.source.publiclink.password=${PUBLICLINK_DB_PASSWORD}
backup.source.user.url=jdbc:mysql://${USER_DB_HOST}:${USER_DB_PORT:3306}/user_schema?useSSL=true&serverTimezone=UTC&readOnly=true
backup.source.user.username=${USER_DB_USERNAME}
backup.source.user.password=${USER_DB_PASSWORD}
backup.storage.base-path=${BACKUP_PATH:/var/backups/publiclink}
backup.publiclink.service-url=${PUBLICLINK_SERVICE_URL}
logging.level.com.qrpublic.apartment.databackup=INFO
